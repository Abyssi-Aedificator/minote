Version 1.6.1
  - Added: find and replace in editor (Ctrl+F / Ctrl+H)
  - Changed: changelog moved to changelog.txt (removed from in-app modal)
  - Fixed: dev banner no longer flashes briefly on load when disabled

Version 1.6.0
- Added: folder icons — choose from a predefined icon set
- Added: tags section in the sidebar with note counts
- Added: click a tag in the sidebar to filter notes
- Added: word and character count visible in editor footer
- Added: undo/redo support (Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y)
- Added: find and replace in editor (Ctrl+F / Ctrl+H)

Version 1.5.3
- Added: three-panel layout with separate folder and note panels
- Added: both panels are resizable by dragging the divider; widths persist across sessions
- Added: mobile folder panel as slide-in overlay
- Fixed: search bar right edge aligns with note panel border

Version 1.5.2
- Fixed: pressing Enter in plain-text (txt) mode no longer corrupts multi-line content on Chromium-based browsers
- Fixed: pasting multi-line text in plain-text mode now correctly preserves line breaks
- Fixed: entry list no longer jitters when selecting a note (border-left no longer shifts padding)
- Fixed: empty-state message in archived/trash views now respects search and image filters
- Fixed: tag names containing HTML are no longer interpreted as markup (XSS)
- Fixed: folder names containing HTML are no longer interpreted as markup in the command palette (XSS)
- Robustness: nodeToPlainText handles block elements as newline boundaries

Version 1.5.1
- Fixed: pressing Enter in plain-text (txt) mode no longer corrupts multi-line content on Chromium-based browsers
- Fixed: pasting multi-line text in plain-text mode now correctly preserves line breaks
- Fixed: entry list no longer jitters when selecting a note (border-left no longer shifts padding)
- Fixed: empty-state message in archived/trash views now respects search and image filters
- Robustness: nodeToPlainText handles block elements as newline boundaries

Version 1.5.0
- Toggle button (txt) in the editor header to switch between rendered markdown and raw plain-text editing
- Plain-text (txt) mode fully wired: caret and placeholder are preserved, paste and slash commands no longer break it, and it resets when switching notes
- New notes created while viewing "no folder" are no longer hidden or given a phantom folder
- Folder rename Escape now cancels instead of committing
- Swipe gestures no longer trigger a stray tap that opens the wrong note
- Command palette: Export and Import backup work directly, and single-key shortcuts (n, t, f, s, 1, 2, 3, e, i) are now active
- Dropbox sync now merges by id (offline edits are preserved) with a conflict prompt, uploads on launch, and flushes pending saves on tab close
- Focus mode keeps an on-screen exit on touch devices; accent colour now applies to the logo mark

Version 1.4.1
- New notes now automatically inherit the currently selected folder
- Folder icon shown below the note number for notes that belong to a folder

Version 1.4.0
- Typewriter / Focus Mode — full-screen distraction-free editing with centered text, dimmed non-active lines, and live word/character count at the bottom of the viewport
- Spring-based micro-interactions on pin toggles, buttons, and colour swatches for a polished, tactile feel
- Swipe gestures on mobile — swipe right to quickly pin a note, swipe left to archive or restore it
- Command palette (Ctrl+K) — fuzzy-search palette for actions: new note, toggle theme, open settings, jump to folder, switch view, export backup, and toggle focus mode
- Notes can now be assigned a colour; a colour dot appears in the note list and a colour picker is available in the editor header
- Folder rename button — a pencil icon appears on hover next to each folder, offering an explicit rename entry alongside the existing double-click
- Fixed: creating a folder no longer produces a duplicate entry due to the blur event firing after Enter

Version 1.3.1
- Mobile topbar buttons (changelog, settings) moved above the search bar and aligned to the right
- Theme system now supports Light, Dark, and System (follows OS preference) — browser chrome colour updates accordingly
- Editor: typing with text selected now correctly replaces the selection instead of preserving it

Version 1.3.0
- Full responsive layout — the app adapts to any screen size
- Touch-friendly interaction with larger tap targets and swipe gestures
- Optimised editor and note list for narrow screens
- Bottom bar navigation replaces the sidebar on phones
- Changelog reworked — all versions now visible in a single scrollable view instead of tabbed panels

Version 1.2.0
- Notes now render live markdown as you type, instead of a separate edit/preview toggle
- Markdown syntax (bold, headings, quotes, links, code fences, etc.) stays hidden until you put the cursor on that line
- The note list preview snippet now shows plain text instead of raw markdown syntax

Version 1.0.0
- Full release